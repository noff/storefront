<%# Devise общается с пользователем только через flash — без этого блока он молчит. %>
<% flash_styles = { "notice" => "alert-success", "alert" => "alert-danger", "error" => "alert-danger" } %>

<% flash.each do |type, message| %>
  <% next if message.blank? %>
  <div class="alert <%= flash_styles.fetch(type.to_s, "alert-info") %> alert-dismissible fade show" role="alert">
    <%= message %>
    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Закрыть"></button>
  </div>
<% end %>
